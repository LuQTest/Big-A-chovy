# AI 情绪层 · 方案存档（暂不落地）

> 2026-08-04 决策：**不落地**。两周后若"信号执行都对、仍觉缺维度"再回看本文件。

## 背景
框架 100% 规则驱动，无叙事/消息面维度。曾评估引入"AI 资讯/情绪层"作为参考加权项（只调仓位、不进信号判定）。

## 调研结论（2026-08-04，librarian + oracle 双路）

### go-stock（ArvinLovegood/go-stock，7.1k star）
- 完整桌面应用（Wails+Vue），非库，不可抽离；Windows 优先，macOS 受限；GPL-3.0
- **逐条电报情绪是词典规则（gse 分词），不是 LLM**；LLM 仅用于"市场新闻总结"
- 自带 AI 选股体系，与"情绪层不做第二信号"底线冲突
- 可参考：数据源端点（财联社/新浪/东方财富/雪球）

### 否决理由（今日暴露的根因）
1. 瓶颈在执行（不在场/感觉单），不在信息——生产侧增量无用
2. T+1 周期吃不到个股新闻情绪——消息当天已反映在价格/资金流
3. 与 7/30 收敛纯规则的防腐哲学相斥，参考项会变质为借口

### 若未来重启（设计要点，Oracle 方案）
- 挂载点：盘后 15:30 对已过量化门禁候选做标注，追加报告 `## AI情绪参考`
- 单向棘轮：情绪分只能调仓位乘数 0.5×/1.0×/1.25×，confidence=low 静音；永不改变信号判定
- 边界：排除公告 avoid/watch_risk（已有规则覆盖）、技术面、股价预测、社交媒体噪音
- 四道防腐锁：进程隔离 → 单向写入 → 审计日志 → 月度相关性统计（不合格冻结一月）
- 选型：自建 ~150 行 Python（东方财富新闻 API + DeepSeek），不用 go-stock；成本约 ¥0.01/日

### 唯一即时可做的残渣
突发风险报警器：盘中关键词扫描（监管调查/实控人失联/强制退市）+ 桌面通知。
10 行规则，不需要 LLM。**等信号消费跑通后再考虑。**

## 触发条件（重启检查清单）
- [ ] /盘中 skill 连续使用 ≥2 周
- [ ] 执行率（决策清单执行比例）可统计且 >50%
- [ ] 复盘记录显示"信号全对但总缺什么"的案例出现
